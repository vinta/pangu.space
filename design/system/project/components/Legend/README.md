The key to the diff colors. It sits in a card's strip and shows only while the diff is on.

- The consumer supplies the two labels.
- Each item is a 12px swatch, `a` for added and `d` for removed, then its label in the `legend` type style.
- The swatches use the same `a` and `d` classes as the marks in the diff rows.

The 40px header above a pane. A label on the left, and at most one small button on the right.

- The consumer supplies the label, and the button if the pane has an action.
- The label is the `label` type style in `color-text-secondary`. A `color-border-light` divider sits under it.
- When the pane is a text area, make the label a real `label` element pointing at it.
- Under 640px it grows to 44px.
